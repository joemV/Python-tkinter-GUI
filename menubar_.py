from tkinter import Menu

''' Create a Menu Bar that will take in arguments for setting the names of the
items that will be displayed onto the screen
Not the Menu titles themselves but the items that go into the titles '''

""" Blue print for obtaining the Menu items by using dictionaries instead of
setting them up one by one """

"""
         for words in menu_labels:
		       #Cascade items:
		       words
		       #Add Command
		       menu_labels[words]

"""
